# SE-SDK

# install

add this code to your composer.json
  
``` bash
"repositories":[
      {
          "type": "vcs",
          "url" : "git@bitbucket.org:smartexpert/se-sdk.git"
      }
  ]
```

add to require:

``` bash
"se/sdk": "dev-master"
```
run

``` bash
composer update
```
